[build]
  command = "pnpm --filter @agnes/web build"
  publish = "apps/web/dist"
  # Vite 8 / rolldown requires Node >= 20.19 or >= 22.12
  [build.environment]
    NODE_VERSION = "22"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
